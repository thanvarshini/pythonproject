# pythonproject
This Python program acts as a simple CCTV system using a webcam. It detects motion by comparing video frames, highlights movement with rectangles and saves recordings with timestamps. A welcome message is shown using a decorator and users can control it via keyboard prompts.
